$(document).ready(function () {
   /*  $("#PersonaId").select2({
        width: '100%'
    })
        .on("select2:select", function (e) {
            console.log('evento en linea')
            var data = e.params.data;
            $("#nombres").val($(data.element).data('nombres'));
            $("#apellidos").val($(data.element).data('apellidos'));
            $("#direccion").val($(data.element).data('direccion'));
        }); */
});
$(document).ready(function () {
    $("#RolIds").select2({
        width: '100%'
    })
        .on("select2:select", function (e) {

        });
});